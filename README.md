# Vue 3 + Vite + Replicate.js

For private development and testing, use the lightweight proxy.
A proxy is necessary to avoid CORS issues with the Replicate HTTP API.

get a token from https://replicate.com/api

`node ./cors-proxy.js`
`npm run dev`
